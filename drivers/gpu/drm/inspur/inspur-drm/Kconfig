config DRM_INSPUR
    tristate "DRM Support for Inspur"
    depends on DRM && PCI && MMU
    select DRM_KMS_HELPER
    select DRM_TTM
    help
	Choose this option if you have a Inspur soc chipset.If M is selected the
	module will be called inspur-drm.
